# TODO

- [ ] Dedupe pnpm PATH setup in `.zshrc` — local hardcoded block (`/Users/sunny/.local/share/pnpm`) may now be redundant with upstream's `PNPM_HOME`-driven env shim from the Makefile installer.
