---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - REVIEW_AND_RECOMMENDATIONS.md
autonomous: true

must_haves:
  truths:
    - "New visitor reads README and understands the repo manages zsh, git, tmux, nvim configs via GNU Stow"
    - "User can follow platform-specific instructions to install on macOS, Linux, or WSL"
    - "README accurately reflects current repo structure (stow/ directory with 4 packages, Makefile targets, test suite)"
    - "No outdated documentation references remain (no bash, no runcom/, no config/, no bin/, no dot command, no remote-install.sh)"
  artifacts:
    - path: "README.md"
      provides: "Complete rewrite reflecting simplified dotfiles structure"
      contains: "stow/"
    - path: "REVIEW_AND_RECOMMENDATIONS.md"
      provides: "DELETED — obsolete pre-simplification document"
  key_links:
    - from: "README.md"
      to: "Makefile"
      via: "Installation instructions reference actual make targets"
      pattern: "make (install|macos|linux|wsl)"
    - from: "README.md"
      to: "stow/"
      via: "Structure section documents actual packages"
      pattern: "stow/(zsh|git|tmux|nvim)"
---

<objective>
Rewrite README.md to accurately document the simplified dotfiles repository and remove the obsolete REVIEW_AND_RECOMMENDATIONS.md.

Purpose: The current README references infrastructure that no longer exists (bash support, runcom/config directories, bin/ scripts, dot command, remote install, language package managers). A visitor to this repo would be confused. The README must reflect what actually exists now.

Output: A complete, accurate README.md and removal of REVIEW_AND_RECOMMENDATIONS.md.
</objective>

<execution_context>
@/home/sunny/.claude/get-shit-done/workflows/execute-plan.md
@/home/sunny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Makefile
@stow/zsh/.zshenv
@stow/zsh/.config/zsh/.zshrc
@stow/zsh/.config/zsh/.zimrc
@stow/git/.config/git/config
@.github/workflows/ci.yml
@.github/Dockerfile.ubuntu
@test/test_helper.bash
@REMOVED.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>README.md</files>
  <action>
Completely rewrite README.md. Do NOT patch the existing file — start fresh. The current content references infrastructure that was deleted in Phases 1-4.

Structure the new README with these sections:

**Title and intro:**
- Title: ".files" (keep original name)
- One-liner: Personal dotfiles for zsh, neovim, tmux, and git. Managed with GNU Stow.
- Platforms: macOS (Intel/ARM), Linux (Ubuntu/Debian), WSL2

**What's included:**
- Brief list of the 4 Stow packages: zsh (Zim framework, modular config), git (aliases, XDG-compliant), tmux (gpakosz/.tmux), nvim (lazy.nvim, LSP via mason)
- Mention the 8 Homebrew packages: git, neovim, tmux, stow, zsh, fzf, ripgrep, bat

**Repository structure:**
Show the actual tree:
```
.
├── Makefile              # Cross-platform installer
├── stow/
│   ├── zsh/              # Shell config (ZDOTDIR=~/.config/zsh)
│   ├── git/              # Git config (~/.config/git/)
│   ├── tmux/             # Tmux config (~/.config/tmux/)
│   └── nvim/             # Neovim config (~/.config/nvim/)
├── test/                 # BATS test suite
├── .github/              # CI workflows + Dockerfile
├── REMOVED.md            # What was deleted and why
└── CONTRIBUTING.md       # How to add new tools
```

**Installation:**
Per-platform sections (collapsible details blocks). For each platform:
- macOS: Prerequisites (xcode-select --install), then `git clone` + `make install`
- Linux: Prerequisites (apt install curl git make), then `git clone` + `make install`
- WSL: Same as Linux (auto-detected)

Clone path should be flexible — use `~/.dotfiles` as the example but note it works from anywhere.

Post-install for all: `git config --global user.name` and `git config --global user.email`

**How it works:**
Explain briefly: Makefile installs Homebrew, installs packages, runs `stow -R -d stow -t $HOME zsh git tmux nvim`, sets zsh as default shell. Stow creates symlinks from the package directories into $HOME.

**Customization:**
- Key files to edit: .zshrc for shell, git config for git, init.lua for neovim, tmux.conf.local for tmux
- Point to CONTRIBUTING.md for adding new tool packages

**Testing:**
`make test` runs BATS test suite. CI runs on push/PR via GitHub Actions (Ubuntu Docker + macOS native).

**Uninstall:**
`make unlink` removes symlinks.

**Credits:**
Keep brief. Mention dotfiles community.

IMPORTANT constraints:
- Do NOT reference: bash, runcom/, config/ directory, bin/ directory, system/ directory, dot command, remote-install.sh, npmfile, Rustfile, Caskfile, Brewfile (as separate file), language package managers
- Do NOT use emojis in headings (use plain text section headers)
- Keep total length under 200 lines — this is personal dotfiles, not a framework
- Use fenced code blocks for all commands
- All paths must reflect actual current structure
  </action>
  <verify>
Verify the new README:
1. `wc -l README.md` — should be under 200 lines
2. Grep for removed concepts that should NOT appear: `grep -E '(runcom|config/|bin/is-|dot (help|test|clean)|remote-install|npmfile|Rustfile|Caskfile|bashrc|bash_profile)' README.md` — should return nothing
3. Grep for required concepts that MUST appear: `grep -c 'stow/' README.md` — should be > 0; `grep -c 'make install' README.md` — should be > 0; `grep -c 'CONTRIBUTING' README.md` — should be > 0
  </verify>
  <done>README.md accurately describes current repository with installation instructions, structure overview, and no references to deleted infrastructure</done>
</task>

<task type="auto">
  <name>Task 2: Remove REVIEW_AND_RECOMMENDATIONS.md</name>
  <files>REVIEW_AND_RECOMMENDATIONS.md</files>
  <action>
Delete REVIEW_AND_RECOMMENDATIONS.md from the repository root. This is a pre-simplification document from the initial project review that references the old architecture (bin/is-macos scripts, system/.path, bash profiles, runcom/ directory). All its recommendations have either been implemented or explicitly rejected during Phases 1-5. Keeping it would confuse visitors.

Use `git rm REVIEW_AND_RECOMMENDATIONS.md` to stage the deletion.

Do NOT add an entry to REMOVED.md for this file — REMOVED.md documents user-facing config files that were removed, not planning/review artifacts.
  </action>
  <verify>`test ! -f REVIEW_AND_RECOMMENDATIONS.md && echo "DELETED"` — should print DELETED</verify>
  <done>REVIEW_AND_RECOMMENDATIONS.md no longer exists in the repository</done>
</task>

</tasks>

<verification>
1. README.md exists and is under 200 lines
2. README.md contains accurate installation instructions for macOS, Linux, WSL
3. README.md references current structure (stow/, Makefile, test/, .github/)
4. README.md does NOT reference deleted infrastructure
5. REVIEW_AND_RECOMMENDATIONS.md is deleted
</verification>

<success_criteria>
A new visitor to the repository can read the README and:
- Understand what the repo manages (zsh, git, tmux, nvim via Stow)
- Install on their platform by following the instructions
- Know where to look for customization
- Find no references to infrastructure that no longer exists
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-01-SUMMARY.md`
</output>
