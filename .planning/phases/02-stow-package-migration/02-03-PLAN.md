---
phase: 02-stow-package-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - nvim/.config/nvim/init.lua
  - nvim/.config/nvim/lua/plugins/lsp.lua
  - nvim/.config/nvim/lua/plugins/editor.lua
  - nvim/.config/nvim/lua/plugins/ui.lua
autonomous: true

must_haves:
  truths:
    - "nvim/ Stow package exists with Lua config at nvim/.config/nvim/"
    - "init.lua bootstraps lazy.nvim and sets essential vim options"
    - "LSP support configured with mason.nvim for automatic server installation"
    - "Treesitter provides syntax highlighting"
    - "Basic file navigation available (telescope or similar)"
    - "Plugin specs are organized in lua/plugins/ directory (one file per concern)"
  artifacts:
    - path: "nvim/.config/nvim/init.lua"
      provides: "Neovim entry point with lazy.nvim bootstrap and core settings"
      contains: "lazy.nvim"
      min_lines: 30
    - path: "nvim/.config/nvim/lua/plugins/lsp.lua"
      provides: "LSP configuration with mason"
      contains: "nvim-lspconfig"
    - path: "nvim/.config/nvim/lua/plugins/editor.lua"
      provides: "Treesitter, telescope, and editing plugins"
      contains: "nvim-treesitter"
    - path: "nvim/.config/nvim/lua/plugins/ui.lua"
      provides: "UI enhancements (statusline, colorscheme)"
      contains: "return"
  key_links:
    - from: "nvim/.config/nvim/init.lua"
      to: "nvim/.config/nvim/lua/plugins/"
      via: "lazy.nvim import directive"
      pattern: "import.*plugins"
    - from: "nvim/.config/nvim/lua/plugins/lsp.lua"
      to: "mason.nvim"
      via: "mason-lspconfig bridge"
      pattern: "mason-lspconfig"
---

<objective>
Create the nvim/ Stow package with a lightweight Lua configuration using lazy.nvim and LSP support.

Purpose: Establishes a from-scratch Neovim config (requirement NVM-01) with lazy.nvim plugin management (requirement NVM-02). The config is lightweight, self-explanatory (STR-03), and provides LSP, syntax highlighting, and file navigation out of the box. This is NOT a distribution like LazyVim/NvChad — it's a minimal, readable config the user fully controls.

Output: nvim/ Stow package ready for `stow nvim`, plugins auto-install on first launch
</objective>

<execution_context>
@/home/sunny/.claude/get-shit-done/workflows/execute-plan.md
@/home/sunny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stow-package-migration/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create init.lua with lazy.nvim bootstrap and core settings</name>
  <files>
    nvim/.config/nvim/init.lua
  </files>
  <action>
    Create `nvim/.config/nvim/init.lua` as the Neovim entry point.

    1. Create directory: `mkdir -p nvim/.config/nvim/lua/plugins`

    2. Write `nvim/.config/nvim/init.lua` with these sections (in order):

    **Section 1: Leader key** (must be set before lazy.nvim loads)
    ```lua
    vim.g.mapleader = " "
    vim.g.maplocalleader = "\\"
    ```

    **Section 2: lazy.nvim bootstrap** (auto-install if missing)
    Use the exact pattern from research:
    ```lua
    local lazypath = vim.fn.stdpath("data") .. "/lazy/lazy.nvim"
    if not (vim.uv or vim.loop).fs_stat(lazypath) then
      local lazyrepo = "https://github.com/folke/lazy.nvim.git"
      vim.fn.system({ "git", "clone", "--filter=blob:none", "--branch=stable", lazyrepo, lazypath })
    end
    vim.opt.rtp:prepend(lazypath)
    ```

    **Section 3: lazy.nvim setup**
    ```lua
    require("lazy").setup({
      spec = {
        { import = "plugins" },
      },
      ui = { border = "rounded" },
      checker = { enabled = false },
      change_detection = { notify = false },
    })
    ```

    **Section 4: Core vim options** (essential settings only, well-commented)
    - `vim.opt.number = true` — line numbers
    - `vim.opt.relativenumber = true` — relative line numbers
    - `vim.opt.mouse = "a"` — enable mouse
    - `vim.opt.clipboard = "unnamedplus"` — system clipboard
    - `vim.opt.breakindent = true` — wrapped lines respect indent
    - `vim.opt.undofile = true` — persistent undo
    - `vim.opt.ignorecase = true` — case insensitive search
    - `vim.opt.smartcase = true` — unless uppercase used
    - `vim.opt.signcolumn = "yes"` — always show sign column
    - `vim.opt.updatetime = 250` — faster CursorHold
    - `vim.opt.splitright = true` — vertical splits to right
    - `vim.opt.splitbelow = true` — horizontal splits below
    - `vim.opt.expandtab = true` — spaces over tabs
    - `vim.opt.shiftwidth = 2` — 2-space indent
    - `vim.opt.tabstop = 2` — 2-space tab
    - `vim.opt.termguicolors = true` — 24-bit color
    - `vim.opt.scrolloff = 8` — keep 8 lines above/below cursor
    - `vim.opt.cursorline = true` — highlight current line

    **Section 5: Core keymaps** (essential only, no plugin keymaps)
    - Clear search highlight: `vim.keymap.set("n", "<Esc>", "<cmd>nohlsearch<CR>")`
    - Window navigation: `<C-h/j/k/l>` for moving between splits
    - Diagnostic navigation: `[d` and `]d` for prev/next diagnostic

    Keep this file UNDER 80 lines. Every line should be self-explanatory. Add section comments.
  </action>
  <verify>
    - `ls nvim/.config/nvim/init.lua` exists
    - `ls nvim/.config/nvim/lua/plugins/` directory exists
    - `grep 'lazy.nvim' nvim/.config/nvim/init.lua` shows bootstrap
    - `grep 'mapleader' nvim/.config/nvim/init.lua` shows leader set before lazy
    - File is under 80 lines: `wc -l nvim/.config/nvim/init.lua`
  </verify>
  <done>
    init.lua exists with lazy.nvim bootstrap, core settings, and essential keymaps. Plugin directory structure ready. File is concise and self-explanatory.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create plugin specs for LSP, editor features, and UI</name>
  <files>
    nvim/.config/nvim/lua/plugins/lsp.lua
    nvim/.config/nvim/lua/plugins/editor.lua
    nvim/.config/nvim/lua/plugins/ui.lua
  </files>
  <action>
    Create three plugin spec files in `nvim/.config/nvim/lua/plugins/`. Each returns a Lua table of plugin specs for lazy.nvim.

    **1. `lua/plugins/lsp.lua`** — LSP, completion, and formatting
    ```lua
    return {
      -- LSP Configuration
      {
        "neovim/nvim-lspconfig",
        dependencies = {
          "williamboman/mason.nvim",
          "williamboman/mason-lspconfig.nvim",
        },
        config = function()
          require("mason").setup()
          require("mason-lspconfig").setup({
            ensure_installed = { "lua_ls", "ts_ls", "jsonls" },
          })

          local lspconfig = require("lspconfig")
          local on_attach = function(_, bufnr)
            local map = function(keys, func, desc)
              vim.keymap.set("n", keys, func, { buffer = bufnr, desc = "LSP: " .. desc })
            end
            map("gd", vim.lsp.buf.definition, "Go to definition")
            map("gr", vim.lsp.buf.references, "Go to references")
            map("K", vim.lsp.buf.hover, "Hover documentation")
            map("<leader>rn", vim.lsp.buf.rename, "Rename symbol")
            map("<leader>ca", vim.lsp.buf.code_action, "Code action")
          end

          local servers = { "lua_ls", "ts_ls", "jsonls" }
          for _, server in ipairs(servers) do
            lspconfig[server].setup({ on_attach = on_attach })
          end

          -- Lua LSP needs special config for Neovim API
          lspconfig.lua_ls.setup({
            on_attach = on_attach,
            settings = {
              Lua = {
                runtime = { version = "LuaJIT" },
                workspace = { library = vim.api.nvim_get_runtime_file("", true) },
                telemetry = { enable = false },
              },
            },
          })
        end,
      },

      -- Autocompletion
      {
        "hrsh7th/nvim-cmp",
        event = "InsertEnter",
        dependencies = {
          "hrsh7th/cmp-nvim-lsp",
          "hrsh7th/cmp-buffer",
          "hrsh7th/cmp-path",
          "L3MON4D3/LuaSnip",
          "saadparwaiz1/cmp_luasnip",
        },
        config = function()
          local cmp = require("cmp")
          local luasnip = require("luasnip")
          cmp.setup({
            snippet = { expand = function(args) luasnip.lsp_expand(args.body) end },
            mapping = cmp.mapping.preset.insert({
              ["<C-n>"] = cmp.mapping.select_next_item(),
              ["<C-p>"] = cmp.mapping.select_prev_item(),
              ["<C-y>"] = cmp.mapping.confirm({ select = true }),
              ["<C-Space>"] = cmp.mapping.complete(),
            }),
            sources = {
              { name = "nvim_lsp" },
              { name = "luasnip" },
              { name = "buffer" },
              { name = "path" },
            },
          })
        end,
      },
    }
    ```

    **2. `lua/plugins/editor.lua`** — Treesitter, Telescope, and editing essentials
    ```lua
    return {
      -- Syntax highlighting via Treesitter
      {
        "nvim-treesitter/nvim-treesitter",
        build = ":TSUpdate",
        config = function()
          require("nvim-treesitter.configs").setup({
            ensure_installed = { "lua", "javascript", "typescript", "json", "yaml", "markdown", "bash", "vim", "vimdoc" },
            highlight = { enable = true },
            indent = { enable = true },
          })
        end,
      },

      -- Fuzzy finder
      {
        "nvim-telescope/telescope.nvim",
        branch = "0.1.x",
        dependencies = { "nvim-lua/plenary.nvim" },
        keys = {
          { "<leader>ff", "<cmd>Telescope find_files<cr>", desc = "Find files" },
          { "<leader>fg", "<cmd>Telescope live_grep<cr>", desc = "Live grep" },
          { "<leader>fb", "<cmd>Telescope buffers<cr>", desc = "Buffers" },
          { "<leader>fh", "<cmd>Telescope help_tags<cr>", desc = "Help tags" },
        },
      },

      -- Commenting
      { "numToStr/Comment.nvim", opts = {} },

      -- Surround
      { "kylechui/nvim-surround", event = "VeryLazy", opts = {} },

      -- Autopairs
      { "windwp/nvim-autopairs", event = "InsertEnter", opts = {} },
    }
    ```

    **3. `lua/plugins/ui.lua`** — Colorscheme, statusline, and visual aids
    ```lua
    return {
      -- Colorscheme
      {
        "catppuccin/nvim",
        name = "catppuccin",
        priority = 1000,
        config = function()
          vim.cmd.colorscheme("catppuccin")
        end,
      },

      -- Statusline
      {
        "nvim-lualine/lualine.nvim",
        dependencies = { "nvim-tree/nvim-web-devicons" },
        opts = {
          options = {
            theme = "catppuccin",
            component_separators = "|",
            section_separators = "",
          },
        },
      },

      -- Indent guides
      {
        "lukas-reineke/indent-blankline.nvim",
        main = "ibl",
        opts = {},
      },

      -- Git signs in gutter
      {
        "lewis6991/gitsigns.nvim",
        opts = {},
      },

      -- Which-key (shows keybinding hints)
      {
        "folke/which-key.nvim",
        event = "VeryLazy",
        opts = {},
      },
    }
    ```

    Each file should be self-explanatory with clear comments. Keep total plugin count reasonable (under 20 plugins). Every plugin should serve an obvious purpose.
  </action>
  <verify>
    - `ls nvim/.config/nvim/lua/plugins/lsp.lua` exists
    - `ls nvim/.config/nvim/lua/plugins/editor.lua` exists
    - `ls nvim/.config/nvim/lua/plugins/ui.lua` exists
    - `grep 'nvim-lspconfig' nvim/.config/nvim/lua/plugins/lsp.lua` confirms LSP plugin
    - `grep 'nvim-treesitter' nvim/.config/nvim/lua/plugins/editor.lua` confirms treesitter
    - `grep 'telescope' nvim/.config/nvim/lua/plugins/editor.lua` confirms file navigation
    - `stow -n -v nvim 2>&1` dry run succeeds
    - Total plugin count is under 20
  </verify>
  <done>
    Three plugin spec files created covering LSP (with mason auto-install), editor features (treesitter, telescope, commenting, surround, autopairs), and UI (colorscheme, statusline, indent guides, gitsigns, which-key). All plugins auto-install on first Neovim launch via lazy.nvim. `stow -n nvim` succeeds.
  </done>
</task>

</tasks>

<verification>
1. nvim/.config/nvim/ has init.lua and lua/plugins/ directory
2. init.lua bootstraps lazy.nvim and loads plugins from lua/plugins/
3. LSP configured with mason for automatic server installation
4. Treesitter provides syntax highlighting for common languages
5. Telescope provides file finding and text search
6. Colorscheme, statusline, and visual aids configured
7. `stow -n nvim` dry run succeeds
8. Total config is under 200 lines across all files
</verification>

<success_criteria>
- nvim/ Stow package exists with correct directory structure
- `stow -n nvim` succeeds without errors
- init.lua is concise (under 80 lines) with lazy.nvim bootstrap
- Plugin specs organized by concern (lsp, editor, ui)
- LSP support with mason for auto-installing language servers
- Treesitter for syntax highlighting
- Telescope for file navigation
- All files are self-explanatory (STR-03 requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/02-stow-package-migration/02-03-SUMMARY.md`
</output>
