---
phase: 02-stow-package-migration
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - config/alacritty/alacritty.toml
  - config/alacritty/alacritty.yml
  - config/prettier/.prettierrc
  - config/thefuck/settings.py
  - config/topgrade.toml
autonomous: false

must_haves:
  truths:
    - "Unused tool configs (alacritty, prettier, thefuck, topgrade) are removed from repository"
    - "Old empty directories (runcom/, system/, config/) are removed"
    - "All four Stow packages (zsh, git, tmux, nvim) can be stowed independently without errors"
    - "Repository root shows clean package-per-tool structure"
  artifacts:
    - path: "zsh/.zshenv"
      provides: "Zsh package exists (created by Plan 01)"
    - path: "git/.config/git/config"
      provides: "Git package exists (created by Plan 02)"
    - path: "tmux/.config/tmux/tmux.conf"
      provides: "Tmux package exists (created by Plan 02)"
    - path: "nvim/.config/nvim/init.lua"
      provides: "Nvim package exists (created by Plan 03)"
  key_links:
    - from: "stow zsh"
      to: "~/.zshenv and ~/.config/zsh/*"
      via: "GNU Stow symlink creation"
      pattern: "LINK"
    - from: "stow git"
      to: "~/.config/git/*"
      via: "GNU Stow symlink creation"
      pattern: "LINK"
    - from: "stow tmux"
      to: "~/.config/tmux/*"
      via: "GNU Stow symlink creation"
      pattern: "LINK"
    - from: "stow nvim"
      to: "~/.config/nvim/*"
      via: "GNU Stow symlink creation"
      pattern: "LINK"
---

<objective>
Remove unused tool configs, clean up empty directories, and verify all Stow packages work.

Purpose: Completes CLN-02 (remove unused tool configs) and ensures the repository has a clean package-per-tool structure. After Plans 01-03 moved files into Stow packages, the old directories (runcom/, system/, config/) should be empty or contain only files to delete. This plan removes those remnants and runs a full stow dry-run verification across all packages.

Output: Clean repository with only zsh/, git/, tmux/, nvim/ packages and infrastructure files
</objective>

<execution_context>
@/home/sunny/.claude/get-shit-done/workflows/execute-plan.md
@/home/sunny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stow-package-migration/02-01-SUMMARY.md
@.planning/phases/02-stow-package-migration/02-02-SUMMARY.md
@.planning/phases/02-stow-package-migration/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused tool configs and empty directories</name>
  <files>
    config/alacritty/alacritty.toml
    config/alacritty/alacritty.yml
    config/prettier/.prettierrc
    config/thefuck/settings.py
    config/topgrade.toml
  </files>
  <action>
    Remove all unused tool configurations per CLN-02 requirement: thefuck, topgrade, zoxide, fzf, delta, alacritty, prettier, duti.

    1. Remove unused config files with `git rm`:
       - `git rm config/alacritty/alacritty.toml config/alacritty/alacritty.yml` (alacritty - out of scope per PROJECT.md)
       - `git rm config/prettier/.prettierrc` (prettier - being removed)
       - `git rm config/thefuck/settings.py` (thefuck - being removed)
       - `git rm config/topgrade.toml` (topgrade - being removed)

    2. After removals, the entire `config/` directory should be empty (git/ and tmux/ subdirs were emptied by Plan 02). Remove it: if `config/` has no tracked files, `git rm -r config/` or just `rmdir` the empty dirs.

    3. Remove empty `runcom/` directory if it exists and is empty (Plan 01 moved zsh files, Plan 02 moved tmux.conf.local). Use `rmdir runcom/` or `git rm -r runcom/` if git-tracked empty.

    4. Remove empty `system/` directory if it still exists (Plan 01 should have handled .dir_colors move). Use `rmdir system/` if empty.

    5. Verify clean state: `ls` at repo root should show:
       - zsh/ git/ tmux/ nvim/ (Stow packages)
       - bin/ install/ macos/ test/ .github/ .vscode/ (infrastructure - Phase 4 will handle)
       - Makefile, README.md, .editorconfig, .gitignore, .stow-local-ignore, remote-install.sh, REVIEW_AND_RECOMMENDATIONS.md, TESTING.md

    NOTE: Do NOT remove bin/, install/, macos/, test/, .github/, Makefile, or other infrastructure files yet. Those are Phase 4 (Installation & Cleanup) scope.
  </action>
  <verify>
    - `! test -d config/` — config/ directory removed
    - `! test -d runcom/` — runcom/ directory removed
    - `! test -d system/` — system/ directory removed
    - `git status` shows clean removals committed
    - `ls -d */ | sort` at repo root includes: bin/ git/ install/ macos/ nvim/ test/ tmux/ zsh/
  </verify>
  <done>
    All unused tool configs removed (alacritty, prettier, thefuck, topgrade). Empty directories (config/, runcom/, system/) cleaned up. Repository has clean package-per-tool structure alongside infrastructure directories.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Stow packages work correctly</name>
  <files>none (verification only)</files>
  <action>
    Run dry-run stow commands for all four packages and display results to user for verification:

    ```bash
    cd ~/projects/dotfiles
    echo "=== zsh package ==="
    stow -n -v zsh 2>&1
    echo "=== git package ==="
    stow -n -v git 2>&1
    echo "=== tmux package ==="
    stow -n -v tmux 2>&1
    echo "=== nvim package ==="
    stow -n -v nvim 2>&1
    echo "=== repo structure ==="
    ls -la
    echo "=== zsh package contents ==="
    find zsh/ -type f | sort
    echo "=== git package contents ==="
    find git/ -type f | sort
    echo "=== tmux package contents ==="
    find tmux/ -type f | sort
    echo "=== nvim package contents ==="
    find nvim/ -type f | sort
    ```

    Present the output to the user and wait for approval.
  </action>
  <verify>
    User reviews the dry-run output and confirms:
    1. `stow -n zsh` would create symlinks at ~/.zshenv and ~/.config/zsh/
    2. `stow -n git` would create symlinks at ~/.config/git/
    3. `stow -n tmux` would create symlinks at ~/.config/tmux/
    4. `stow -n nvim` would create symlinks at ~/.config/nvim/
    5. No Stow errors or conflicts
    6. Repository root looks clean with package-per-tool structure
    7. No leftover config/, runcom/, system/ directories

    Optional: user can actually stow with `stow --no-folding zsh git tmux nvim` and verify symlinks.
  </verify>
  <done>
    User approves the Stow package structure and dry-run results. All four packages (zsh, git, tmux, nvim) verified working. Phase 2 complete.
  </done>
</task>

</tasks>

<verification>
1. No unused tool config files remain (alacritty, prettier, thefuck, topgrade gone)
2. No empty legacy directories remain (config/, runcom/, system/ gone)
3. All four Stow packages pass dry-run without errors
4. Repository structure shows clean package-per-tool layout
5. Human verifies stow output looks correct
</verification>

<success_criteria>
- CLN-02 complete: unused tool configs removed
- All legacy directories (config/, runcom/, system/) removed
- `stow -n zsh git tmux nvim` all succeed without errors
- Repository root has clean package-per-tool structure
- Human approves the final structure
</success_criteria>

<output>
After completion, create `.planning/phases/02-stow-package-migration/02-04-SUMMARY.md`
</output>
