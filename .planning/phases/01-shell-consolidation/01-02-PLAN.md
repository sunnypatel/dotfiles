---
phase: 01-shell-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - runcom/.bash_profile
  - runcom/.bashrc
  - runcom/.inputrc
  - system/.alias
  - system/.alias.macos
  - system/.completion
  - system/.completion.bash
  - system/.completion.zsh
  - system/.env
  - system/.env.bash
  - system/.env.macos
  - system/.env.zsh
  - system/.fix
  - system/.function
  - system/.function_fs
  - system/.function.macos
  - system/.function_network
  - system/.function_text
  - system/.fzf
  - system/.grep
  - system/.nvm
  - system/.path
  - system/.prompt
  - system/.zoxide
  - system/.java
autonomous: false

must_haves:
  truths:
    - "No bash config files remain in runcom/ (.bash_profile, .bashrc removed)"
    - "No unused system/ files remain (.prompt, .completion.bash, .function_network, etc. removed)"
    - "The runcom/ directory contains only Zsh files and tmux config"
    - "Zsh starts and loads aliases, completion, and prompt without errors"
  artifacts:
    - path: "runcom/"
      provides: "Clean directory with only .zshenv, .zshrc, .zimrc, tmux.conf.local"
    - path: "system/"
      provides: "Only .dir_colors remains (used by dircolors for ls colors)"
  key_links:
    - from: "runcom/.zshrc"
      to: "shell startup"
      via: "No broken references to removed files"
      pattern: "source.*system/"
---

<objective>
Remove all bash configuration files and unused shell system files, leaving only the new Zsh configs from Plan 01.

Purpose: Complete the CLN-01 (remove bash config) and CLN-03 (remove unused shell functions) requirements. After this, the shell configuration is fully Zsh-only with no dead files.

Output: Clean runcom/ and system/ directories with only active files remaining.
</objective>

<execution_context>
@/home/sunny/.claude/get-shit-done/workflows/execute-plan.md
@/home/sunny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-consolidation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove bash config files from runcom/</name>
  <files>
    runcom/.bash_profile
    runcom/.bashrc
    runcom/.inputrc
  </files>
  <action>
Delete these bash-specific files from runcom/:

1. **runcom/.bash_profile** -- The main bash entry point. All useful content has been migrated to .zshenv and .zshrc in Plan 01.
2. **runcom/.bashrc** -- Bash runtime config. Only sources .bash_profile and adds pnpm PATH (already in .zshenv).
3. **runcom/.inputrc** -- GNU Readline config. Only used by bash; Zsh uses its own input system (Zimfw input module handles keybindings).

Also delete the swap file if present:
4. **runcom/.zshrc.swp** -- Editor swap file, should not be in repo.

Use `git rm` for tracked files so git records the deletion.

```bash
git rm runcom/.bash_profile runcom/.bashrc runcom/.inputrc
rm -f runcom/.zshrc.swp
```

After deletion, runcom/ should contain ONLY:
- .zshenv (from Plan 01)
- .zshrc (from Plan 01)
- .zimrc (from Plan 01)
- tmux.conf.local (kept for Phase 2)
  </action>
  <verify>
`ls runcom/` should show exactly: .zimrc, .zshenv, .zshrc, tmux.conf.local (4 files).
  </verify>
  <done>runcom/.bash_profile, .bashrc, and .inputrc are deleted via git rm. No bash config files remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused system/ shell files</name>
  <files>
    system/.alias
    system/.alias.macos
    system/.completion
    system/.completion.bash
    system/.completion.zsh
    system/.env
    system/.env.bash
    system/.env.macos
    system/.env.zsh
    system/.fix
    system/.function
    system/.function_fs
    system/.function.macos
    system/.function_network
    system/.function_text
    system/.fzf
    system/.grep
    system/.nvm
    system/.path
    system/.prompt
    system/.zoxide
    system/.java
  </files>
  <action>
Delete all system/ files whose content has been migrated to the new .zshrc/.zshenv or is no longer needed. Use `git rm` for each.

**Files migrated to .zshenv (Plan 01):**
- system/.path -- PATH logic now in .zshenv
- system/.env -- environment vars now split between .zshenv and .zshrc
- system/.env.bash -- bash-specific, no longer needed
- system/.env.zsh -- zsh options now in .zshrc
- system/.env.macos -- macOS env vars now conditionally in .zshrc

**Files migrated to .zshrc (Plan 01):**
- system/.alias -- aliases now in .zshrc
- system/.alias.macos -- macOS aliases now conditionally in .zshrc
- system/.function -- mk() and calc() now in .zshrc
- system/.grep -- grep alias/options now in .zshrc

**Files no longer needed (CLN-03 / out of scope tools):**
- system/.completion -- bash completion logic, Zimfw handles zsh completion
- system/.completion.bash -- bash-only completion
- system/.completion.zsh -- was empty/minimal, Zimfw handles this
- system/.fix -- only contained disabled thefuck alias
- system/.function_fs -- unused file system utilities (duf, gz, dataurl, etc.)
- system/.function.macos -- unused macOS functions (cdf, bundleid)
- system/.function_network -- unused network utilities (srv, transfer, unshorten)
- system/.function_text -- unused text utilities (line, duplines, uniqlines)
- system/.fzf -- fzf integration, tool being removed
- system/.nvm -- nvm loading, out of scope
- system/.prompt -- bash prompt, replaced by Zimfw asciiship
- system/.zoxide -- zoxide integration, tool being removed
- system/.java -- Java environment, out of scope

```bash
git rm system/.alias system/.alias.macos \
  system/.completion system/.completion.bash system/.completion.zsh \
  system/.env system/.env.bash system/.env.macos system/.env.zsh \
  system/.fix system/.function system/.function_fs system/.function.macos \
  system/.function_network system/.function_text \
  system/.fzf system/.grep system/.java system/.nvm \
  system/.path system/.prompt system/.zoxide
```

**Files to KEEP in system/:**
- system/.dir_colors -- still useful for terminal color definitions, referenced by dircolors caching if needed. Will be evaluated in Phase 2 for migration to zsh/ package.

After deletion, system/ should contain ONLY:
- .dir_colors
  </action>
  <verify>
`ls system/` should show exactly: .dir_colors (1 file). Run `git status` to confirm all deletions are staged.
  </verify>
  <done>All 22 unused system/ files deleted via git rm. Only .dir_colors remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Zsh-only shell configuration works</name>
  <action>
Complete Zsh-only shell configuration has been built:
- New .zshenv with platform-aware PATH and environment variables
- New .zshrc with Zimfw init, aliases, functions, history, and completion
- New .zimrc with curated module declarations
- Removed all bash config files (.bash_profile, .bashrc, .inputrc)
- Removed all unused system/ files (22 files)

User must verify the shell loads correctly.

**Verification steps:**

1. Verify file structure:
   ```
   ls runcom/
   # Expected: .zimrc  .zshenv  .zshrc  tmux.conf.local
   ls system/
   # Expected: .dir_colors
   ```

2. Test new shell config (without affecting current shell):
   ```
   zsh -n runcom/.zshenv && echo "zshenv OK"
   zsh -n runcom/.zshrc && echo "zshrc OK"

   ZDOTDIR=/home/sunny/projects/dotfiles/runcom zsh -i -c 'echo "Shell loaded"; alias g; alias reload; echo "EDITOR=$EDITOR"'
   ```

3. Verify startup performance (should be well under 100ms):
   ```
   for i in 1 2 3; do time ZDOTDIR=/home/sunny/projects/dotfiles/runcom zsh -i -c exit; done
   ```

4. Verify git status shows clean deletions and modifications:
   ```
   git status
   ```

Note: On first run, Zimfw may need to download modules which takes a few seconds. Subsequent starts will be fast.
  </action>
  <verify>User confirms shell loads with working aliases, prompt, and completion.</verify>
  <done>User has verified Zsh shell starts correctly with all expected features working.</done>
</task>

</tasks>

<verification>
1. `ls runcom/` -- exactly 4 files: .zimrc, .zshenv, .zshrc, tmux.conf.local
2. `ls system/` -- exactly 1 file: .dir_colors
3. `git status` -- shows all deletions staged, no untracked files
4. `zsh -n runcom/.zshenv` -- no syntax errors
5. `zsh -n runcom/.zshrc` -- no syntax errors
6. No references to bash_profile in any remaining file: `grep -r 'bash_profile' runcom/ system/` returns nothing
</verification>

<success_criteria>
- Zero bash config files in runcom/ (CLN-01 complete)
- Zero unused function files in system/ (CLN-03 complete)
- system/ contains only .dir_colors
- Shell starts without errors using new config
- User has verified shell startup with aliases, prompt, and completion working
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-consolidation/01-02-SUMMARY.md`
</output>
