---
phase: 04-installation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - REMOVED.md
  - macos/defaults.sh
  - macos/defaults-chrome.sh
  - macos/dock.sh
  - remote-install.sh
  - install/Brewfile
  - install/Caskfile
  - install/Codefile
  - install/duti
  - install/npmfile
  - install/Rustfile
autonomous: true

must_haves:
  truths:
    - "Running make (or make install) twice in a row produces no errors"
    - "No macOS defaults scripts exist in the repository"
    - "No remote install script exists in the repository"
    - "No language package manager files (npmfile, Rustfile) exist in the repository"
    - "No install/ directory exists in the repository"
    - "REMOVED.md at repo root documents every deleted file with rationale"
    - "Makefile is under 50 total lines"
  artifacts:
    - path: "REMOVED.md"
      provides: "Deletion history and rationale for all removed files"
      contains: "macOS Defaults Scripts"
    - path: "Makefile"
      provides: "Idempotent cross-platform installation"
      exports: ["all", "install", "macos", "linux", "wsl", "unlink"]
  key_links:
    - from: "Makefile"
      to: "Brewfile"
      via: "brew bundle --file"
      pattern: "brew bundle.*Brewfile"
    - from: "Makefile"
      to: "stow/"
      via: "stow -d stow command"
      pattern: "stow -d.*stow"
---

<objective>
Remove all obsolete installation infrastructure (macOS defaults scripts, remote install script, language package managers, old install/ directory), create REMOVED.md documenting deletions, and finalize the Makefile for idempotent operation under 50 lines.

Purpose: Complete the strip-down of the repository by removing the last traces of the bloated multi-tool setup. Makefile becomes the single, clean entry point for installation.
Output: Clean repo with only essential files, REMOVED.md at root, and a finalized Makefile.
</objective>

<execution_context>
@/home/sunny/.claude/get-shit-done/workflows/execute-plan.md
@/home/sunny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-installation-cleanup/04-RESEARCH.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove obsolete files and create REMOVED.md</name>
  <files>
    macos/defaults.sh
    macos/defaults-chrome.sh
    macos/dock.sh
    remote-install.sh
    install/Brewfile
    install/Caskfile
    install/Codefile
    install/duti
    install/npmfile
    install/Rustfile
    REMOVED.md
  </files>
  <action>
    Delete these files and directories using git rm:
    - `git rm macos/defaults.sh macos/defaults-chrome.sh macos/dock.sh` then `rmdir macos/`
    - `git rm remote-install.sh`
    - `git rm install/Brewfile install/Caskfile install/Codefile install/duti install/npmfile install/Rustfile` then `rmdir install/`

    Create REMOVED.md at the repository root documenting all deletions. Follow the template from the research document. Structure:

    1. Header explaining the file's purpose (what was deleted and why, prevents reintroduction)
    2. Section per requirement grouping:
       - CLN-04: macOS Defaults Scripts -- defaults.sh (381 lines), defaults-chrome.sh (11 lines), dock.sh (11 lines). Rationale: fragile across macOS versions, settings change silently, manual System Settings is more reliable.
       - CLN-05: Remote Install Script -- remote-install.sh (36 lines). Rationale: curl-to-bash is security anti-pattern, unnecessary for personal dotfiles. Clone + make install is the right pattern.
       - CLN-06: Language Package Managers -- install/npmfile (17 packages), install/Rustfile (3 packages). Rationale: language package management is not a dotfiles concern, belongs in project-level config.
       - Old install/ directory -- install/Brewfile (56 lines), install/Caskfile (20 lines), install/Codefile (9 lines), install/duti (91 lines). Rationale: replaced by root-level Brewfile (8 packages). Casks, VS Code extensions, and file associations managed separately.
    3. Each entry: file path, line count, rationale, "do not reintroduce" guidance.

    Also include a brief section for files removed in earlier phases (Phase 1-3) -- at minimum note that bash configs, bin/ scripts, system/ directory, config/ directory, and unused tool configs were removed. Reference git log for exact details. Keep this section concise (a few bullet points per phase).
  </action>
  <verify>
    Run: `test ! -d macos && test ! -f remote-install.sh && test ! -d install && test -f REMOVED.md && echo "PASS" || echo "FAIL"`
    Run: `grep -c "CLN-04\|CLN-05\|CLN-06" REMOVED.md` should return 3 or more (all requirements documented).
  </verify>
  <done>
    macos/ directory, remote-install.sh, and install/ directory are gone from the repo. REMOVED.md exists at root with structured documentation for every deleted file including rationale and "do not reintroduce" guidance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize Makefile -- add install target and trim to under 50 lines</name>
  <files>Makefile</files>
  <action>
    The current Makefile is 54 lines and already well-structured with .PHONY, brew guard, and idempotent commands. Make these targeted changes:

    1. Add an `install` alias target that maps to `all` -- users expect `make install` to work. Add `install` to the .PHONY line and add `install: all` after the `all: $(OS)` line.

    2. Trim total line count to under 50 by:
       - Remove the blank line between `wsl: linux` and `install-deps:` (line ~35)
       - Remove one blank line between targets where two consecutive blank lines exist
       - Keep ALL comments -- do not sacrifice readability for line count
       - The Makefile must remain readable; only remove unnecessary whitespace

    3. Verify the `install-deps` target has `-y` flag on apt-get (already present) for non-interactive operation.

    4. Do NOT change any command logic -- the Makefile works correctly. This is cosmetic + adding the install alias.

    Final Makefile should be 48-50 total lines with the `install` target added.
  </action>
  <verify>
    Run: `wc -l Makefile` should show under 50 lines.
    Run: `make -n` should show the targets that would execute without errors (dry run).
    Run: `grep "^install:" Makefile` should show the install target exists.
    Run: `grep ".PHONY" Makefile | grep "install"` should confirm install is declared phony.
  </verify>
  <done>
    Makefile is under 50 total lines, has an `install` alias target, all existing targets work unchanged, and `make -n` shows correct execution plan.
  </done>
</task>

</tasks>

<verification>
1. `make -n` completes without errors (dry-run of default target)
2. `test ! -d macos && test ! -f remote-install.sh && test ! -d install` -- no obsolete files
3. `test -f REMOVED.md` -- documentation exists
4. `wc -l Makefile` shows under 50 lines
5. `grep "install" Makefile` shows install target
6. `grep -c "CLN-04\|CLN-05\|CLN-06" REMOVED.md` confirms all requirements documented
</verification>

<success_criteria>
- make install runs without errors (idempotent -- can re-run safely)
- Makefile is under 50 total lines with clear targets for macOS and Linux
- No macOS defaults scripts, remote install scripts, or language package managers remain in repo
- REMOVED.md at repo root documents all deleted files with rationale and "do not reintroduce" guidance
</success_criteria>

<output>
After completion, create `.planning/phases/04-installation-cleanup/04-01-SUMMARY.md`
</output>
