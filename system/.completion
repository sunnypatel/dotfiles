BREW_PREFIX=$(brew --prefix)


# If bash shell load bash completions
if ps -p $$ -o comm= | grep -q "bash"; then
  . "$DOTFILES_DIR"/system/.completion.bash
elif ps -p $$ -o comm= | grep -q "zsh"; then
  . "$DOTFILES_DIR"/system/.completion.zsh
fi

